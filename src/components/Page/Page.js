import React from 'react';

// Utils
import classnames from 'classnames';

const Page = () => (
  <div className={classnames('ui-page')}>
    Hello World
  </div>
);

Page.propTypes = {
};

Page.defaultProps = {
};

export default Page;
